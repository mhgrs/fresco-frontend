# URL de la API del backend.
# En producción apunta al dominio de Cloudflare Workers (frescopos.cl/api).
# En desarrollo local, omitir esta variable — cae en http://localhost:8000/api/ por defecto.
VITE_API_URL=https://www.frescopos.cl/api
