// Roles del sistema — usar siempre estas constantes, nunca strings literales.
// Ejemplo: usuario.roles?.includes(ROLES.ADMIN)

export const ROLES = {
  ADMIN:      'ADMIN',
  CAJERO:     'CAJERO',
  BODEGA:     'BODEGA',
  SUPERVISOR: 'SUPERVISOR',
};
